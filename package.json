{
    "name": "the-village-vault",
    "version": "1.0.0",
    "description": "Clash of Clans Village Analytics",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@vercel/analytics": "^1.4.0",
        "@vercel/postgres": "^0.8.0"
    },
    "engines": {
        "node": ">=18"
    }
}